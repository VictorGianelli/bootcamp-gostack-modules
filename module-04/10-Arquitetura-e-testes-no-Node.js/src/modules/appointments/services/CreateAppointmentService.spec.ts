import CreateAppointmentService from './CreateAppointmentService';

describe('CreateAppointment', () => {
  it('should be able to create a mew appointment', () => {
    expect(2 + 2).toBe(4)
  });

  // it('should not be able to create two appointment to the same time', () => {
  //   expect(2 + 2).toBe(4)
  // });
})